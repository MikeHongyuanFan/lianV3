from django.db import models

# No models needed for reports app as it will use existing models
# This app will focus on providing reporting endpoints that aggregate data
# from other models in the system
