/**
 * CRM Loan Management System API
 * A comprehensive CRM system for loan applications with fully synchronized frontend and backend development.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RepaymentRequest model module.
 * @module models/RepaymentRequest
 * @version 1.0.0
 */
class RepaymentRequest {
    /**
     * Constructs a new <code>RepaymentRequest</code>.
     * Serializer for repayments
     * @alias module:models/RepaymentRequest
     * @param amount {Number} 
     * @param application {Number} 
     */
    constructor(amount, application) { 
        
        RepaymentRequest.initialize(this, amount, application);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, application) { 
        obj['amount'] = amount;
        obj['application'] = application;
    }

    /**
     * Constructs a <code>RepaymentRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:models/RepaymentRequest} obj Optional instance to populate.
     * @return {module:models/RepaymentRequest} The populated <code>RepaymentRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RepaymentRequest();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('due_date')) {
                obj['due_date'] = ApiClient.convertToType(data['due_date'], 'Date');
            }
            if (data.hasOwnProperty('paid_date')) {
                obj['paid_date'] = ApiClient.convertToType(data['paid_date'], 'Date');
            }
            if (data.hasOwnProperty('invoice')) {
                obj['invoice'] = ApiClient.convertToType(data['invoice'], File);
            }
            if (data.hasOwnProperty('application')) {
                obj['application'] = ApiClient.convertToType(data['application'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RepaymentRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RepaymentRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RepaymentRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }

        return true;
    }


}

RepaymentRequest.RequiredProperties = ["amount", "application"];

/**
 * @member {Number} amount
 */
RepaymentRequest.prototype['amount'] = undefined;

/**
 * @member {Date} due_date
 */
RepaymentRequest.prototype['due_date'] = undefined;

/**
 * @member {Date} paid_date
 */
RepaymentRequest.prototype['paid_date'] = undefined;

/**
 * @member {File} invoice
 */
RepaymentRequest.prototype['invoice'] = undefined;

/**
 * @member {Number} application
 */
RepaymentRequest.prototype['application'] = undefined;






export default RepaymentRequest;

