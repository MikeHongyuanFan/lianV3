/**
 * CRM Loan Management System API
 * A comprehensive CRM system for loan applications with fully synchronized frontend and backend development.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BorrowerList model module.
 * @module models/BorrowerList
 * @version 1.0.0
 */
class BorrowerList {
    /**
     * Constructs a new <code>BorrowerList</code>.
     * Serializer for listing borrowers with minimal information
     * @alias module:models/BorrowerList
     * @param id {Number} 
     * @param createdAt {Date} 
     * @param applicationCount {Number} 
     */
    constructor(id, createdAt, applicationCount) { 
        
        BorrowerList.initialize(this, id, createdAt, applicationCount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, createdAt, applicationCount) { 
        obj['id'] = id;
        obj['created_at'] = createdAt;
        obj['application_count'] = applicationCount;
    }

    /**
     * Constructs a <code>BorrowerList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:models/BorrowerList} obj Optional instance to populate.
     * @return {module:models/BorrowerList} The populated <code>BorrowerList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BorrowerList();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('first_name')) {
                obj['first_name'] = ApiClient.convertToType(data['first_name'], 'String');
            }
            if (data.hasOwnProperty('last_name')) {
                obj['last_name'] = ApiClient.convertToType(data['last_name'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('phone')) {
                obj['phone'] = ApiClient.convertToType(data['phone'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('application_count')) {
                obj['application_count'] = ApiClient.convertToType(data['application_count'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BorrowerList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BorrowerList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BorrowerList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['first_name'] && !(typeof data['first_name'] === 'string' || data['first_name'] instanceof String)) {
            throw new Error("Expected the field `first_name` to be a primitive type in the JSON string but got " + data['first_name']);
        }
        // ensure the json data is a string
        if (data['last_name'] && !(typeof data['last_name'] === 'string' || data['last_name'] instanceof String)) {
            throw new Error("Expected the field `last_name` to be a primitive type in the JSON string but got " + data['last_name']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['phone'] && !(typeof data['phone'] === 'string' || data['phone'] instanceof String)) {
            throw new Error("Expected the field `phone` to be a primitive type in the JSON string but got " + data['phone']);
        }

        return true;
    }


}

BorrowerList.RequiredProperties = ["id", "created_at", "application_count"];

/**
 * @member {Number} id
 */
BorrowerList.prototype['id'] = undefined;

/**
 * @member {String} first_name
 */
BorrowerList.prototype['first_name'] = undefined;

/**
 * @member {String} last_name
 */
BorrowerList.prototype['last_name'] = undefined;

/**
 * @member {String} email
 */
BorrowerList.prototype['email'] = undefined;

/**
 * @member {String} phone
 */
BorrowerList.prototype['phone'] = undefined;

/**
 * @member {Date} created_at
 */
BorrowerList.prototype['created_at'] = undefined;

/**
 * @member {Number} application_count
 */
BorrowerList.prototype['application_count'] = undefined;






export default BorrowerList;

